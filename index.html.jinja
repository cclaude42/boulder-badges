<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Badges</title>
    <link rel="icon" href="src/favicon.png">
    <link rel="stylesheet" href="styles.css">
    <link href="https://fonts.googleapis.com/css2?family=Press+Start+2P&display=swap" rel="stylesheet">
</head>
<body>
    {% for profile in profiles %}
    <div class="container {{ profile.name }}{{ ' visible' if profile.default }}">
        <div class="title">
            <button class="text arrow" onclick="prevProfile()"><</button>
            <div class="text profile">{{ profile.name.title() }}</div>
            <button class="text arrow" onclick="nextProfile()">></button>
        </div>
        <div class="image-box">
            <img src="src/logo.png" alt="Logo">
            <img src="src/{{ 'badge' if 'white'  in profiles.badges else 'empty' }}-white.png"  alt="White badge">
            <img src="src/{{ 'badge' if 'orange' in profiles.badges else 'empty' }}-orange.png" alt="Orange badge">
            <img src="src/{{ 'badge' if 'green'  in profiles.badges else 'empty' }}-green.png"  alt="Green badge">
            <img src="src/{{ 'badge' if 'blue'   in profiles.badges else 'empty' }}-blue.png"   alt="Blue badge">
            <img src="src/{{ 'badge' if 'red'    in profiles.badges else 'empty' }}-red.png"    alt="Red badge">
            <img src="src/{{ 'badge' if 'black'  in profiles.badges else 'empty' }}-black.png"  alt="Black badge">
            <img src="src/{{ 'badge' if 'purple' in profiles.badges else 'empty' }}-purple.png" alt="Purple badge">
        </div>
    </div>
    {% endfor %}
    <script src="script.js"></script>
</body>
</html>
